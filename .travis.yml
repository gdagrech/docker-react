language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t gagrech/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true gagrech/docker-react npm run test -- --coverage